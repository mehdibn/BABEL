export KAFKA_PATH="{{ kafka_install_parent_dir }}/kafka"
export KAFKA_BROKERS="{{ kafkabrokers }}"
export ZOOKEEPER_QUORUM="{{ zookeeperquorum }}"



{{kafka_install_parent_dir}}/kafka/bin/kafka-topics.sh --zookeeper $ZOOKEEPER_QUORUM --create --topic {{ kafkaproducertopic }} --replication-factor {{ kafkarep }} --partitions 1
{{kafka_install_parent_dir}}/kafka/bin/kafka-topics.sh --zookeeper $ZOOKEEPER_QUORUM --create --topic {{ kafkaconsumertopic }} --replication-factor {{ kafkarep }} --partitions 1



{{kafka_install_parent_dir}}/kafka/bin/kafka-topics.sh --zookeeper $ZOOKEEPER_QUORUM --describe

