export KAFKA_PATH="{{ kafka_install_parent_dir }}/kafka"
export KAFKA_BROKERS="{{ kafkabrokers }}"
export ZOOKEEPER_QUORUM="{{ zookeeperquorum }}"



{{kafka_install_link_dir}}/bin/kafka-topics.sh --zookeeper $ZOOKEEPER_QUORUM --create --topic bdbench-p --replication-factor 1 --partitions 1
{{kafka_install_link_dir}}/bin/kafka-topics.sh --zookeeper $ZOOKEEPER_QUORUM --create --topic bdbench-c --replication-factor 1 --partitions 1



{{kafka_install_link_dir}}/bin/kafka-topics.sh --zookeeper $ZOOKEEPER_QUORUM --describe

